services:
  db:
    image: postgres:latest
    ports:
      - "5430:5432"
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - 8888:8080
